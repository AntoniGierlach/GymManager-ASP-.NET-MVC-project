@model GymManager.Models.Club
@{
    ViewData["Title"] = "Usuń oddział";
}

<div class="page-header">
    <h2 class="page-title"><i class="fa-solid fa-trash"></i> Usuń oddział</h2>
    <a class="btn btn-outline-light" asp-action="Index">
        <i class="fa-solid fa-arrow-left me-2"></i>Wróć
    </a>
</div>

<div class="card">
    <div class="card-body">
        <div class="alert alert-danger">
            Na pewno chcesz usunąć oddział <strong>@Model.Name</strong>?
        </div>

        <dl class="row">
            <dt class="col-sm-4 text-light-emphasis">Adres</dt>
            <dd class="col-sm-8">@Model.Address</dd>
            <dt class="col-sm-4 text-light-emphasis">Telefon</dt>
            <dd class="col-sm-8">@Model.ContactPhone</dd>
            <dt class="col-sm-4 text-light-emphasis">Email</dt>
            <dd class="col-sm-8">@Model.ContactEmail</dd>
        </dl>

        <form asp-action="Delete" method="post" class="d-flex justify-content-end gap-2">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger px-4">
                <i class="fa-solid fa-trash me-2"></i>Usuń
            </button>
            <a class="btn btn-outline-light px-4" asp-action="Index">Anuluj</a>
        </form>
    </div>
</div>
